# Local development overrides
# This file is loaded by Vite and takes precedence over .env

# For local development without Docker, proxy /api through Vite to avoid CORS.
# If you have Docker running, you can change this to: http://localhost:54321/functions/v1/api
VITE_API_URL=
